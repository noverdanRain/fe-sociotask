import { motion as m } from "motion/react";
import { Box, Button, Flex } from "@chakra-ui/react"

export const MotionBox = m(Box);
export const MotionFlex = m(Flex);
export const MotionButton = m(Button);
